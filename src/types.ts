export type Movies = {
    attributes: {
        title: string,
        poster: string,
        release_date: string,
        running_time: string,
        trailer: string,
        summary: string,
    }
}